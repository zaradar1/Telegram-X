streamlit>=1.28.0
telethon>=1.34.0
requests>=2.31.0
aiohttp>=3.9.0
pandas>=2.0.0
python-dotenv>=1.0.0
cryptography>=41.0.0
pyTelegramBotAPI>=4.13.0
tqdm>=4.66.0

# Optional cloud upload targets (Bulk Downloader "Also upload each file to").
# Inert unless S3_BUCKET or GDRIVE_SERVICE_ACCOUNT_JSON is set — safe to
# remove if you don't need either target.
boto3>=1.34.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
